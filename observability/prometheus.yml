global:
  scrape_interval: 15s

scrape_configs:
  # Scrape the collector's Prometheus exporter (the metrics Claude Code sent via OTLP).
  - job_name: claude-code
    static_configs:
      - targets: ['collector:8889']
